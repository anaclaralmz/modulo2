function ordenar (){
    let valores= String(document.querySelector(#string).value)
    let lista = valores.split(",")
    let vFoco = String(document.querySelector(#valorFoco).value)

    var lista.sort();

    if(lista.includes(vFoco)) {
        lista.index0f(vFoco); //pega a posição do item na lista
    }


}